# Load necessary libraries
library(nettskjemar)
library(testthat)

test_check("nettskjemar")

# nolint start
# cov <- covr::package_coverage() # no codecov
# covr::zero_coverage(cov) # no codecov
# nolint end
